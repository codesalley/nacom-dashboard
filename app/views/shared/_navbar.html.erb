<nav class="navbar navbar-dark navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <% if current_admin %>
    <a class="navbar-brand" href="#"
      ><%= current_admin.email.split('@')[0] %></a
    >
    <% else %>
    <a class="navbar-brand" href="#">''</a>

    <% end %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
      <div class="left-nav">
        <% if current_admin %> <%= link_to 'SignOut',
        destroy_admin_session_path, method: :delete, class: 'btn
        btn-outline-danger' %> <% else %> <%= link_to 'SignIn',
        new_admin_session_path, class: 'btn btn-outline-success' %> <% end %>
      </div>
    </div>
  </div>
</nav>
